FROM python:3.11-slim
WORKDIR /app
RUN pip install uv
COPY pyproject.toml .
COPY packages/ packages/
COPY services/api/ services/api/
RUN uv sync --no-dev
EXPOSE 8000
CMD ["uv", "run", "uvicorn", "api.router:app", "--host", "0.0.0.0", "--port", "8000"]
